{{- if .Values.deploy.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace | default "default" }}
  labels:
    name: {{ .Values.namespace | default "default" }}
    app.kubernetes.io/managed-by: argocd
{{- end }}
